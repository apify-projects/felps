{
  "title": "Input Schema",
  "description": "",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Query",
      "type": "string",
      "description": "The text to search for"
    },
    "flow": {
      "title": "Flow",
      "type": "string",
      "description": "The flow to search for",
      "editor": "select",
      "default": "",
      "enum": [""]
    },
    "proxy": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Select proxies to be used by your crawler.",
      "prefill": {
        "useApifyProxy": false
      },
      "editor": "proxy"
    }
  }
}
